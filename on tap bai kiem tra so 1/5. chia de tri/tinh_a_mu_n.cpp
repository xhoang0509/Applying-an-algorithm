// Bài 1: Thiết kế thuật toán tính a^n theo phương pháp chia để trị (a thực, n nguyên dương). Cài đặt
// chương trình ứng dụng thuật toán.

#include <bits/stdc++.h>
using namespace std;

long TinhMu(float a, int n) {
    if (n == 0) {
        return 1;
    } else if (a == 1) {
        return a;
    }
}

int main() {
    return 0;
}